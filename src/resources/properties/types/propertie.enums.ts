export enum PropertyType {
    APARTMENT = 'apartment',
    HOUSE = 'house',
    SHARED = 'shared'
}

export enum PropertyStatus {
    AVAILABLE = 'available',
    PENDING = 'pending',
    TAKEN = 'taken'
}
